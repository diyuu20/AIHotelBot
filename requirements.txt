# Core Flask dependencies
Flask==2.3.3
Werkzeug==2.3.7
gunicorn

# Lightweight image processing (safe for Render)
opencv-python-headless==4.8.1.78
numpy==1.24.3
Pillow==10.0.1

# OCR (optional but lightweight enough)
pytesseract==0.3.10

# Utilities
python-dateutil==2.8.2
pandas==2.0.3